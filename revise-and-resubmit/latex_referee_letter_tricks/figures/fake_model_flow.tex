\documentclass{article}

\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}

%%%<
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{5pt}%
%%%>

\begin{document}


% Define block styles
\tikzstyle{block} = [ellipse, draw, fill=white, 
    text width=6em, text centered, rounded corners, minimum height=4em]
   \tikzstyle{textblock} = [rectangle, draw=white, fill=white, text centered]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{cloud} = [draw, ellipse, fill=white, node distance=3cm,
    minimum height=2em]
    
\begin{tikzpicture}
    % Place nodes
    \node (init) {};
    \node [block,  left of = init, node distance = 6cm] (payer) {\normalsize Government};
    %\node [cloud, above left of =payer, node distance = 5cm] (reinsurer) {\large Re-insurer};
    %\node [block, above of =init, node distance = 5cm] (private) {\large Private ``insurers'' = Administrators};
    \node [block, right of =init, node distance = 3cm] (provider) {\normalsize Firms};
     \node [block, below left of =provider, node distance = 7.5cm] (patient) {\normalsize Workers};
     \node [block, right of =patient, node distance = 6.6cm] (supp) {\small Home production};



    % Draw edges
    %\path (payer.west) edge[line,bend left=20] node[left] {}  (provider.southwest) ;
    
    \path  (patient.west)  edge[line,bend left=20] node[left] {}  (payer.south);
    \path (patient.north east) edge[line,bend right=20] node[left] {} (provider.south) ;
     \path (supp.north) edge[line,bend right=30] node[left] {} (provider.east) ;


     \path (patient.east) edge[line,bend left=10] node[left] {} (supp.west) ;
     
      \path (payer.east) edge[-, bend left=15] (provider.west) ;

     

 	% Place text nodes (above edges)
     %\node  [textblock, below of =reinsurer, node distance = 2cm, text width=3cm, align=center]  (payer_reinsurer_text) {\small premium to cover huge hits} ;
    % \node  [textblock, above of =payer, node distance = 2.5cm, text width=3cm, align=center]  (reinsurer_payer_text) {\small payment in case of huge hit} ;
         \node  [textblock, below of =payer, node distance = 3cm, text width=3cm, align=center]  (payer_patient_text) {\footnotesize Payroll tax over lifetime  \par} ;
       \node  [textblock, below of =  provider, node distance = 2 cm, text width=2cm, align=center]  (payer_patient_text) {\scriptsize Social norms \par} ;
       %\node  [textblock, above of =  patient, node distance = 2.3 cm, text width=2cm, align=center]   {\small Direct payment of premium \footnotesize (about 1/3 of total premium)} ;
          \node  [textblock, right of =  payer, node distance = 4 cm, text width=2.4cm, align=center]   {\scriptsize Via ergodicity  \par} ;
        \node  [textblock, right of =  patient, node distance = 3.3 cm, text width=1.7cm, align=center]   {\scriptsize Peer effects \par} ;
         
           
\end{tikzpicture}


\end{document}